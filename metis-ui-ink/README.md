# @metis/ui-ink

Metis 终端皮（React + Ink）。**只订阅 `@metis/protocol` 事件**，不进 `@metis/core`。

品牌色：teal `#2DD4BF` / sky `#38BDF8` / amber `#F59E0B`。

布局与工具卡模式改编自 [Gemini CLI](https://github.com/google-gemini/gemini-cli)（Apache-2.0），见 `NOTICE`。字标与配色为 Metis 自有。
